import { Text, View } from "react-native";

export default function AddEntry() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Edit app/(tabs)/add_entry.tsx to edit this screen.</Text>
    </View>
  );
}
